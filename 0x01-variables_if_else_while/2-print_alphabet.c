#include <stdio.h>

/**
* * main - Entry points-Lower Alpha
* * Return: Always (Success)
*/

int main(void)

{
	       char loweralpha;

		for (loweralpha = 'a'; loweralpha <= 'z'; loweralpha++)
			putchar(loweralpha);
			putchar('\n');
			return (0);
}
